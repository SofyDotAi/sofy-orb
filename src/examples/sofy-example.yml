description: >
  Example demonstrates how to use connect-to-sofy command
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    sofy: sofy-dot-ai/sofy@0.0.2
  workflows:
    use-my-orb:
      jobs:
        - sample job
        
